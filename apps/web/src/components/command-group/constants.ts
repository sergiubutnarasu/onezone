export const READ_TOOL_NAMES = new Set(["read", "read_file"]);
export const WRITE_TOOL_NAMES = new Set(["write", "create_file", "write_file"]);
export const EDIT_TOOL_NAMES = new Set(["edit", "multiedit", "str_replace"]);

export const SKILL_ARG_METADATA_KEYS = new Set([
  "skillName",
  "skill_name",
  "skill",
  "name",
  "source",
  "repository",
  "repo",
  "mode",
  "action",
  "command",
  "agent",
  "projectId",
]);